DISCORD_TOKEN=your_bot_token_here

# Optional: restrict slash command sync to one guild for instant testing
# (leave empty to sync globally, which can take up to 1h to propagate)
DEV_GUILD_ID=

# SQLite file path (relative to project root, or absolute)
DATABASE_PATH=inazuma.db
